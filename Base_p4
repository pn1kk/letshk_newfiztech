l = []
I = []
sum = 0

while True:
    inp = input().split()
    if inp == []:
        break
    else:
        l.append(inp[0])
        I.append(inp[1])
for i in range(len(l)):
    l[i] = float(l[i].replace(',', '.'))
    I[i] = float(I[i].replace(',', '.'))
    sum += I[i]

##так как предложенный файл состоит из строк, в каждой из которых содержатся два числа -
##значение величины l_i и соответствующее значение спектральной интенсивности I_i,
##то код поочередно строки, пока они не кончаются, и заносит значения в списки l и I соответственно
##параллельно высчитывается сумма всех значений интенсивности, которая потом будет необходима в нормировке.

p = []
for i in range(len(l)):
    p.append(I[i]/sum)

##нормируем каждое значение интенсивности и получаем вероятность появления значения x_i

mean = 0
mean2 = 0
for i in range(len(l)):
    mean += l[i]*p[i]
    mean2 += l[i]*p[i]*l[i]

##вычисляем среднее (матожидание) величины и квадрата величины

var = mean2 - mean**2

##вычисляем дисперсию

std = var**0.5

##вычисляем среднеквадратическое отклонение

print(mean, std)

##выводим ответ
